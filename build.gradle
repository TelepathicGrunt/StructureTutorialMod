plugins {
	alias(libs.plugins.quilt.loom)
	id 'maven-publish'
}

archivesBaseName = project.archives_base_name
version = project.mod_version
group = project.maven_group

repositories {
	maven {	url "https://nexus.resourcefulbees.com/repository/maven-public/" }
}

dependencies {
	minecraft libs.minecraft
	mappings( loom.layered {
		addLayer quiltMappings.mappings("org.quiltmc:quilt-mappings:${libs.versions.quilt.mappings.get()}:v2")
		// officialMojangMappings() // MojMap mappings. Uncomment if you want to use this mapping instead.
	})
	modImplementation libs.quilt.loader
	modImplementation libs.quilted.fabric.api

	// I recommend having CommandStructures on in your development environment as it gives
	// commands for making debugging structures easier. It is super useful!
	// modRuntimeOnly "com.telepathicgrunt:CommandStructures-Quilt:2.0.0+1.18.2"
}

processResources {
	inputs.property "version", project.version

	filesMatching("quilt.mod.json") {
		expand "version": project.version
	}
}

tasks.withType(JavaCompile).configureEach {
	it.options.encoding = 'UTF-8'
	it.options.release.set(17)
}

java {
	sourceCompatibility = JavaVersion.VERSION_17
	targetCompatibility = JavaVersion.VERSION_17
	withSourcesJar()
}

jar {
	from("LICENSE") {
		rename { "${it}_${archivesBaseName}"}
	}
}